<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的網頁作品集</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    
    <style>
        /* --- 基礎設定 --- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: #fffafc; /* 淺粉色背景 */
            color: #555;
            font-family: 'Noto Sans TC', sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* 確保頁面至少跟螢幕一樣高 */
        }

        /* --- 主內容區塊 --- */
        main {
            flex: 1; /* 自動撐開高度，將 Footer 推到底部 */
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
        }

        .dashboard-container {
            display: grid;
            grid-template-columns: 1fr 2fr 1fr; /* 左:選單, 中:螢幕, 右:說明 */
            width: 100%;
            max-width: 1200px;
            min-height: 600px;
            gap: 30px;
            align-items: center;
        }

        /* --- 左側：選單列表 --- */
        .menu-col {
            display: flex;
            flex-direction: column;
            justify-content: center;
            border-right: 2px dashed #ffb6b9; /* 粉紅虛線分隔 */
            padding-right: 20px;
        }

        .menu-item {
            padding: 15px;
            margin: 8px 0;
            cursor: pointer;
            border-radius: 12px;
            font-weight: bold;
            color: #888;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            border: 2px solid transparent; /* 預留邊框空間避免跳動 */
        }

        /* 滑鼠互動效果 */
        .menu-item:hover, .menu-item.active {
            background-color: #fff0f6;
            color: #ff7eb9;
            border-color: #ffb6b9;
            transform: translateX(5px);
            box-shadow: 4px 4px 0px #ffb6b9; /* 像素風硬邊陰影 */
        }

        .menu-item::before {
            content: '♥';
            display: inline-block;
            margin-right: 10px;
            font-size: 14px;
            color: #aee9f9;
            opacity: 0;
            transition: opacity 0.3s;
        }

        .menu-item:hover::before, .menu-item.active::before {
            opacity: 1;
        }

        /* --- 中間：電腦螢幕 --- */
        .monitor-col {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .monitor-frame {
            position: relative;
            width: 100%;
            padding-top: 56.25%; /* 16:9 比例 */
            background: #ffffff;
            border: 8px solid #aee9f9; /* 粉藍邊框 */
            border-radius: 12px;
            box-shadow: 8px 8px 0px #ffb6b9aa; /* 卡通風格陰影 */
        }

        /* 螢幕支架 */
        .monitor-frame::after {
            content: '';
            position: absolute;
            bottom: -30px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 20px;
            background: #aee9f9;
            border-bottom-left-radius: 10px;
            border-bottom-right-radius: 10px;
            z-index: -1;
        }

        .screen {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #f9f9f9;
            overflow: hidden;
            border-radius: 4px;
        }

        .screen img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: opacity 0.4s ease;
        }

        /* --- 右側：作品資訊 --- */
        .info-col {
            padding-left: 20px;
            border-left: 2px dashed #aee9f9; /* 粉藍虛線分隔 */
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .info-title {
            font-family: 'Press Start 2P', cursive; /* 像素字體 */
            font-size: 1.4rem;
            margin-bottom: 20px;
            line-height: 1.5;
            background: linear-gradient(90deg, #ff7eb9 0%, #aee9f9 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(2px 2px 0px rgba(255,182,185,0.5));
        }

        .info-desc {
            line-height: 1.8;
            color: #666;
            margin-bottom: 30px;
            font-size: 1rem;
            text-align: justify;
        }

        .tech-stack {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .tech-tag {
            padding: 6px 15px;
            background: #fff;
            border-radius: 50px;
            font-size: 0.85rem;
            color: #ff7eb9;
            border: 2px solid #ffb6b9;
            font-weight: bold;
            box-shadow: 2px 2px 0px #ffb6b9aa;
        }

        /* --- 手機版 RWD --- */
        @media (max-width: 900px) {
            .dashboard-container {
                grid-template-columns: 1fr;
                gap: 40px;
                min-height: auto;
            }
            .menu-col, .info-col {
                border: none;
                padding: 0;
            }
            .info-title, .info-desc, .tech-stack {
                text-align: center;
                justify-content: center;
            }
        }

        /* --- Footer 樣式 --- */
        .site-footer {
            background: linear-gradient(90deg, #f8e1ff 0%, #aee9f9 100%);
            font-family: 'Press Start 2P', 'Comic Sans MS', cursive, sans-serif;
            padding: 2em 0 1em 0;
            margin-top: auto;
            box-shadow: 0 -4px 24px #ffb6b9aa;
            width: 100%;
        }
        .footer-content {
            max-width: 900px;
            margin: 0 auto;
            display: flex;
            align-items: stretch;
            justify-content: center;
        }
        .footer-contact, .footer-links {
            flex: 1 1 0;
            padding: 0 2em;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .footer-contact h3, .footer-links h3 {
            color: #ff7eb9;
            font-size: 1.2rem;
            margin-bottom: 1em;
            letter-spacing: 2px;
            text-shadow: 1px 1px 6px #fff0f6;
            font-family: 'Press Start 2P', cursive, sans-serif;
        }
        .footer-contact p, .footer-contact a {
            color: #6c6c6c;
            font-size: 0.9rem;
            margin: 0.4em 0;
            text-decoration: none;
            font-family: 'Noto Sans TC', sans-serif;
        }
        .footer-contact a:hover {
            color: #ff7eb9;
            text-decoration: underline;
        }
        .footer-divider {
            width: 2px;
            background: linear-gradient(180deg, #ffb6b9 0%, #aee9f9 100%);
            margin: 0 1em;
            border-radius: 2px;
        }
        .footer-links {
            align-items: center;
        }
        .social-icons {
            display: flex;
            gap: 1.2em;
            margin-top: 0.5em;
        }
        .social-icons a img {
            width: 36px;
            height: 36px;
            filter: drop-shadow(0 2px 8px #ffb6b9aa);
            transition: transform 0.2s, filter 0.2s;
            background: #fff0f6;
            border-radius: 10px;
            padding: 4px;
        }
        .social-icons a:hover img {
            transform: scale(1.15) rotate(-8deg);
            filter: drop-shadow(0 4px 16px #aee9f9aa);
            background: #ffb6b9;
        }
        @media (max-width: 700px) {
            .footer-content {
                flex-direction: column;
                align-items: center;
            }
            .footer-divider {
                width: 80%;
                height: 2px;
                margin: 1em 0;
                background: linear-gradient(90deg, #ffb6b9 0%, #aee9f9 100%);
            }
            .footer-contact, .footer-links {
                padding: 0.5em 0;
                align-items: center;
                text-align: center;
            }
        }
    </style>
</head>
<body>

    <main>
        <div class="dashboard-container">
            
            <div class="menu-col">
                <div class="menu-item active" data-id="1">Project A. 旅遊網站</div>
                <div class="menu-item" data-id="2">Project B. 個人履歷</div>
                <div class="menu-item" data-id="3">Project C. 購物商城</div>
                <div class="menu-item" data-id="4">Project D. 後台管理</div>
            </div>

            <div class="monitor-col">
                <div class="monitor-frame">
                    <div class="screen">
                        <img id="preview-img" src="https://placehold.co/800x450/fff0f6/ff7eb9?text=Project+A" alt="Website Preview">
                    </div>
                </div>
            </div>

            <div class="info-col">
                <h1 id="project-title" class="info-title">旅遊網站</h1>
                <p id="project-desc" class="info-desc">
                    這是本學期的期中作業，主題是介紹台灣的旅遊景點。運用了 Flexbox 進行響應式排版，並加入了簡單的輪播圖功能。
                </p>
                <div id="project-tags" class="tech-stack">
                    <span class="tech-tag">HTML5</span>
                    <span class="tech-tag">CSS3</span>
                    <span class="tech-tag">JavaScript</span>
                </div>
            </div>

        </div>
    </main>

    <footer class="site-footer">
        <div class="footer-content">
            <div class="footer-contact">
                <h3>Contact</h3>
                <p>電話: 09123456789</p>
                <p>Gmail: <a href="mailto:jolin541204@gmail.com">jolin541204@gmail.com</a></p>
                <p>就讀系所<br>國立台北教育大學數位設計系</p>
            </div>
            <div class="footer-divider"></div>
            <div class="footer-links">
                <h3>Link</h3>
                <div class="social-icons">
                    <a href="https://www.facebook.com/" target="_blank" aria-label="Facebook">
                        <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/facebook.svg" alt="Facebook" />
                    </a>
                    <a href="https://www.instagram.com/" target="_blank" aria-label="Instagram">
                        <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/instagram.svg" alt="Instagram" />
                    </a>
                    <a href="https://www.youtube.com/" target="_blank" aria-label="YouTube">
                        <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/youtube.svg" alt="YouTube" />
                    </a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // 自動檢測英文並套用像素字體
        document.querySelectorAll('.footer-contact h3, .footer-links h3, .footer-links a[aria-label]').forEach(el => {
            if (/^[A-Za-z\s]+$/.test(el.textContent.trim())) {
                el.setAttribute('lang', 'en');
            }
        });

        // 作品集資料庫 (請在此替換你的內容與圖片連結)
        const projects = [
            {
                id: 1,
                title: "旅遊網站",
                desc: "這是本學期的期中作業，主題是介紹台灣的旅遊景點。運用了 Flexbox 進行響應式排版，並加入了簡單的輪播圖功能。",
                tags: ["HTML5", "CSS3", "JavaScript"],
                img: "https://placehold.co/800x450/fff0f6/ff7eb9?text=Travel+Website" 
            },
            {
                id: 2,
                title: "個人履歷",
                desc: "採用極簡風格設計的線上履歷，重點在於字體排版與閱讀體驗。使用了 Grid 系統來規劃版面，讓資訊層級分明。",
                tags: ["UI/UX", "Typography", "CSS Grid"],
                img: "https://placehold.co/800x450/e0f7fa/4dd0e1?text=Resume+Page"
            },
            {
                id: 3,
                title: "購物商城",
                desc: "模擬電商的首頁設計，包含商品卡片展示與購物車互動。挑戰了較複雜的 DOM 操作與事件監聽。",
                tags: ["DOM操作", "SCSS", "RWD"],
                img: "https://placehold.co/800x450/fff8e1/ffb74d?text=E-Commerce"
            },
            {
                id: 4,
                title: "後台管理",
                desc: "一個數據儀表板介面，展示數據圖表與表格排版。練習了 CSS Variable 的應用與資料視覺化佈局。",
                tags: ["Dashboard", "Chart.js", "Layout"],
                img: "https://placehold.co/800x450/f3e5f5/ba68c8?text=Admin+Dashboard"
            }
        ];

        // 互動邏輯
        const menuItems = document.querySelectorAll('.menu-item');
        const previewImg = document.getElementById('preview-img');
        const titleEl = document.getElementById('project-title');
        const descEl = document.getElementById('project-desc');
        const tagsEl = document.getElementById('project-tags');

        menuItems.forEach((item, index) => {
            item.addEventListener('mouseover', () => {
                // 更新按鈕狀態
                menuItems.forEach(i => i.classList.remove('active'));
                item.classList.add('active');

                // 獲取資料
                const data = projects[index];

                // 圖片轉場動畫
                previewImg.style.opacity = 0.5;

                setTimeout(() => {
                    // 更新內容
                    previewImg.src = data.img;
                    titleEl.innerText = data.title;
                    descEl.innerText = data.desc;

                    // 重繪標籤
                    tagsEl.innerHTML = ''; 
                    data.tags.forEach(tag => {
                        const span = document.createElement('span');
                        span.className = 'tech-tag';
                        span.innerText = tag;
                        tagsEl.appendChild(span);
                    });

                    previewImg.style.opacity = 1;
                }, 150);
            });
        });
    </script>
</body>
</html>
